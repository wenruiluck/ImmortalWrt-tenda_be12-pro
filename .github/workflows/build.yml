name: Build ImmortalWrt

on:
  push:
    tags:
      - v**
  workflow_dispatch:
    inputs:
      build_config:
        description: Firmware config to build
        required: true
        type: choice
        default: full
        options:
          - full
          - mini
          - both

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-22.04
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJSON(github.event_name == 'push' && '[{"config_name":"full","config_file":"full"}]' || inputs.build_config == 'mini' && '[{"config_name":"mini","config_file":"minimal"}]' || inputs.build_config == 'both' && '[{"config_name":"mini","config_file":"minimal"},{"config_name":"full","config_file":"full"}]' || '[{"config_name":"full","config_file":"full"}]') }}

    env:
      DEBIAN_FRONTEND: noninteractive
      CCACHE_DIR: ${{ github.workspace }}/.ccache
      CCACHE_COMPRESS: "true"
      CCACHE_MAXSIZE: "3000M"
      CCACHE_BASEDIR: ${{ github.workspace }}

    steps:
      - name: Checkout My Repository Source
        uses: actions/checkout@v7.0.0
        with:
          fetch-depth: 0

      - name: Restore Download Cache
        uses: actions/cache@v6.1.0
        with:
          path: dl
          key: ${{ runner.os }}-immortalwrt-dl-v2-${{ matrix.config_name }}-${{ hashFiles(format('defconfig/{0}.config', matrix.config_file)) }}
          restore-keys: |
            ${{ runner.os }}-immortalwrt-dl-v2-${{ matrix.config_name }}-
            ${{ runner.os }}-immortalwrt-dl-v2-

      - name: Restore Compiler Cache
        id: ccache-restore
        uses: actions/cache/restore@v6.1.0
        with:
          path: .ccache
          key: ${{ runner.os }}-immortalwrt-ccache-${{ matrix.config_name }}-${{ github.ref_name }}-${{ hashFiles(format('defconfig/{0}.config', matrix.config_file)) }}-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-immortalwrt-ccache-${{ matrix.config_name }}-${{ github.ref_name }}-${{ hashFiles(format('defconfig/{0}.config', matrix.config_file)) }}-
            ${{ runner.os }}-immortalwrt-ccache-${{ matrix.config_name }}-
            ${{ runner.os }}-immortalwrt-ccache-


      - name: Initialize Build Environment
        run: |
          sudo bash -c 'bash <(curl -s https://build-scripts.immortalwrt.org/init_build_environment.sh)'
          sudo timedatectl set-timezone "Asia/Shanghai"

      - name: Prepare Compiler Cache
        run: |
          set -euo pipefail

          if ! command -v ccache >/dev/null 2>&1; then
            sudo apt-get update
            sudo apt-get install -y ccache
          fi

          mkdir -p "${CCACHE_DIR}" dl
          ccache -M "${CCACHE_MAXSIZE}"
          ccache --zero-stats || true
          ccache --show-config | sed -n '1,20p'

      - name: Update and Install Feeds
        run: |
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: Load Configuration (${{ matrix.config_name }})
        run: |
          CONFIG_FILE="defconfig/${{ matrix.config_file }}.config"

          set_config() {
            local key="$1"
            local value="$2"

            if grep -q "^# ${key} is not set$" .config; then
              sed -i "s|^# ${key} is not set$|${key}=${value}|" .config
            elif grep -q "^${key}=" .config; then
              sed -i "s|^${key}=.*$|${key}=${value}|" .config
            else
              echo "${key}=${value}" >> .config
            fi
          }

          if [ ! -f "$CONFIG_FILE" ]; then
            echo "❌ Config file not found: $CONFIG_FILE"
            exit 1
          fi

          cp -f "$CONFIG_FILE" .config
          echo "✅ Successfully loaded $CONFIG_FILE"

          set_config "CONFIG_CCACHE" "y"

          make defconfig

          echo "📄 Key config:"
          grep -E '^(CONFIG_TARGET_|CONFIG_CCACHE=|CONFIG_PACKAGE_luci-app-ttyd=|CONFIG_PACKAGE_ttyd=)' .config || true

      - name: Download Sources
        run: |
          make download -j"$(nproc)"
          find dl -maxdepth 1 -type f -size -1024c -delete

      - name: Prepare Loyalsoldier GeoData
        run: |
          set -euo pipefail

          mkdir -p files/usr/share/v2ray files/usr/share/dae
          echo "⬇️ Downloading Loyalsoldier geosite.dat and geoip.dat..."
          curl -sSL --retry 3 -o files/usr/share/v2ray/geosite.dat https://github.com/Loyalsoldier/v2ray-rules-dat/releases/latest/download/geosite.dat || \
          curl -sSL --retry 3 -o files/usr/share/v2ray/geosite.dat https://ghproxy.net/https://github.com/Loyalsoldier/v2ray-rules-dat/releases/latest/download/geosite.dat

          curl -sSL --retry 3 -o files/usr/share/v2ray/geoip.dat https://github.com/Loyalsoldier/v2ray-rules-dat/releases/latest/download/geoip.dat || \
          curl -sSL --retry 3 -o files/usr/share/v2ray/geoip.dat https://ghproxy.net/https://github.com/Loyalsoldier/v2ray-rules-dat/releases/latest/download/geoip.dat

          [ -s files/usr/share/v2ray/geosite.dat ]
          [ -s files/usr/share/v2ray/geoip.dat ]

          ln -sf ../v2ray/geosite.dat files/usr/share/dae/geosite.dat
          ln -sf ../v2ray/geoip.dat files/usr/share/dae/geoip.dat
          ls -lh files/usr/share/v2ray/

      - name: Compile Firmware
        run: |
          set -euo pipefail

          ccache --show-stats || true

          if ! make -j"$(nproc)"; then
            echo "::warning::Parallel build failed, retrying with -j1 V=s"
            make -j1 V=s
          fi

          ccache --show-stats || true

      - name: Save Compiler Cache
        if: always() && steps.ccache-restore.outputs.cache-hit != 'true'
        uses: actions/cache/save@v6.1.0
        with:
          path: .ccache
          key: ${{ steps.ccache-restore.outputs.cache-primary-key }}


      - name: Upload Artifacts
        uses: actions/upload-artifact@v7.0.1
        with:
          name: firmware-${{ matrix.config_name }}
          path: bin/targets/mediatek/filogic/*.bin

  publish:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    if: ${{ github.event_name == 'push' && github.ref_type == 'tag' }}
    steps:
      - name: Download All Artifacts
        uses: actions/download-artifact@v8.0.1
        with:
          path: all-firmwares

      - name: Prepare Release Files
        run: |
          set -euo pipefail

          mkdir -p final-release
          find all-firmwares -type f -name '*.bin' -print -exec cp {} final-release/ \;

          if ! find final-release -type f -name '*.bin' -print -quit | grep -q .; then
            echo "No firmware .bin files found in downloaded artifacts" >&2
            find all-firmwares -maxdepth 3 -type f -print >&2
            exit 1
          fi

          ls -lh final-release

      - name: Create Unified Release
        uses: softprops/action-gh-release@v3.0.1
        with:
          make_latest: true
          tag_name: ${{ github.ref_name }}
          files: final-release/*.bin
          fail_on_unmatched_files: true
          draft: false
          body: |
            ## 📦 发布说明

            ### ✨ 构建配置
            - tag 默认发布 **full** 固件。
            - **mini** 仅通过手动 workflow_dispatch 构建。
            - **full**: 完整构建，包含以下额外预装插件：
              - `luci-proto-wireguard`: WireGuard 协议支持
              - `luci-app-wol`: 网络唤醒 (WoL) 功能
              - `luci-i18n-wol-zh-cn`: WoL 中文语言包
              - `ddns-scripts-cloudflare`: Cloudflare DDNS 脚本
              - `luci-app-ddns`: 动态 DNS 配置界面
              - `luci-i18n-ddns-zh-cn`: DDNS 中文语言包
              - `bind-host`: BIND DNS 服务器宿主支持
              - `luci-app-dae`: dae eBPF 高性能透明代理
              - `Loyalsoldier GeoData`: 集成 Loyalsoldier 增强版 `geosite.dat` & `geoip.dat`（内置 `geosite:gfw` 分流规则支持）
              - `kmod-tun` / `kmod-iptunnel` / `kmod-wireguard`: VPN 隧道基础模块
              - `luci-app-ttyd` / `ttyd`: 网页终端
